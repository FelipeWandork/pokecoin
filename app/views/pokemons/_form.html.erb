<%= form_for @pokemon do |pokemon_form| %>
  <% if pokemon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pokemon.errors.count, "error") %> prohibited this pokemon from being saved:</h2>

      <ul>
        <% pokemon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- lista de pokemons da API -->
  <div>
    <%= pokemon_form.label :name, style: "display: block" %>
    <%= pokemon_form.select :name, @pokemons.map{|p|[p['name']]} %>
  </div>

  <div>
    <%= pokemon_form.label :base_experience, style: "display: block" %>
    <%= pokemon_form.text_field :base_experience %>
  </div>

  <div>
    <%= pokemon_form.label :value_satoshi, style: "display: block" %>
    <%= pokemon_form.text_field :value_satoshi %>
  </div>

  <div>
    <%= pokemon_form.label :value_dolar, style: "display: block" %>
    <%= pokemon_form.text_field :value_dolar %>
  </div>
  <!--
  <%= fields_for :wallet, @wallet do |wallet_fields| %>
    <div>
      <%= wallet_fields.label :satoshi, style: "display: block" %>
      <%= wallet_fields.text_field :satoshi %>
    </div>

    <div>
      <%= wallet_fields.label :dollar, style: "display: block" %>
      <%= wallet_fields.text_field :dollar %>
    </div>
  <% end %>
-->
  <div>
    <%= pokemon_form.submit %>
  </div>

<% end %>